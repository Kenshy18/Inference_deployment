# Historical polygon import shim

The former 15,202-line standalone pipeline has been removed. The remaining
file forwards old research entry points to the modular Production optimizer.
It is not used or included by deployed packages.
